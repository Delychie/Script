<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <RootNamespace>FsocietyInjector</RootNamespace>
    <AssemblyName>FsocietyInjector</AssemblyName>
    <ApplicationManifest>app.manifest</ApplicationManifest>

    <!-- The Win32 injection APIs are architecture-sensitive: a 64-bit target
         can only inject a 64-bit DLL into a 64-bit process. Roblox is x64, so
         we build x64 only and never let the runtime fall back to AnyCPU. -->
    <PlatformTarget>x64</PlatformTarget>
    <Platforms>x64</Platforms>

    <Version>1.0.0</Version>
    <Product>fsociety injector</Product>
    <Company>fsociety</Company>
    <Description>A clean, Mr. Robot-themed Windows DLL injector.</Description>
  </PropertyGroup>

</Project>
